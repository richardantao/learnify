import React from "react";
import { Form as Frm} from "reactstrap";

const Form = props => {
    return (
        <Frm onSubmit={props.handleSubmit}>
            
        </Frm>
    );
};

export default Form;